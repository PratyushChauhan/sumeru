# Overview

Funnelit is a local desktop MCP funnel. Add N upstream MCP servers (stdio commands or HTTP URLs) and expose them through one authenticated Streamable HTTP endpoint.

![Configure tab with running badge, endpoint URL, and bearer token](/images/configure.png)

## What you get

- One endpoint at `http://127.0.0.1:7341/mcp`
- Bearer auth (token shown in Configure)
- Three gateway tools that list and call upstream MCPs
- System tray lifecycle (close window to hide; Quit from tray to stop)
- Optional launch at login (starts hidden in the tray)

## Where to go next

Use the left nav: **Guides**, shared **Cookbook** recipes, then platform cookbooks (**Linux** / **macOS**) for install and startup.
